services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wireless-heatmapper-web
    environment:
      HOSTNAME: 0.0.0.0
      NODE_ENV: production
      PORT: 3333
    ports:
      - "3333:3333"
    restart: unless-stopped
